group 'com'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'one.util', name: 'streamex', version: '0.6.6'

    testCompile group: 'junit', name: 'junit', version: '4.12'
}
